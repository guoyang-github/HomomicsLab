apiVersion: v1
kind: Service
metadata:
  name: {{ include "homomicslab.fullname" . }}
  labels:
    {{- include "homomicslab.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    {{- include "homomicslab.selectorLabels" . | nindent 4 }}
