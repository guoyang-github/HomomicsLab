// SLURM-specific overrides
// Included by slurm_conda and slurm_local profiles

process {
    executor = 'slurm'
    clusterOptions = '--export=ALL'
}

executor {
    queueSize = 200
    submitRateLimit = '10/1min'
    pollInterval = '30 sec'
    jobName = { "nf-${task.process}-${task.index}" }
}
