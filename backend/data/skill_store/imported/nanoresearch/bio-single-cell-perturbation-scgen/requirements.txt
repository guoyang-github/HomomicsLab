# scGen perturbation modeling dependencies
# Install with: pip install -r requirements.txt
#
# NOTE: This skill contains TWO implementations:
#   1. pertpy.tl.Scgen (Jax-based, RECOMMENDED) — scripts/python/scgen_analysis.py
#   2. legacy scgen.SCGEN (PyTorch standalone) — scripts/python/core_analysis.py
#
# These two paths have INCOMPATIBLE dependency requirements.
# Choose ONE path and install the corresponding dependencies below.

# ============================================================
# OPTION A: pertpy.tl.Scgen (RECOMMENDED)
# Compatible with modern scvi-tools>=1.0.0. Active maintenance.
# ============================================================
pertpy>=0.9.0

# ============================================================
# OPTION B: legacy standalone scgen (PyTorch)
# LAST UPDATED ~2022. Incompatible with scvi-tools>=1.0.0.
# Requires scvi-tools<0.20.0 to avoid API conflicts.
# ============================================================
# Uncomment the three lines below if using legacy scgen:
# scgen>=2.1.0
# scvi-tools<0.20.0
# torch>=1.10.0

# ============================================================
# Core dependencies (required by both paths)
# ============================================================
scanpy>=1.9.0
anndata>=0.8.0
numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.5.0
seaborn>=0.11.0
scikit-learn>=1.0.0
scipy>=1.7.0
