# Single-cell batch integration dependencies
# Install with: pip install -r requirements.txt

# Core dependencies
scanpy>=1.10.0
anndata>=0.10.0
numpy>=1.26.0
pandas>=2.0.0
matplotlib>=3.8.0
seaborn>=0.13.0

# Integration methods
harmonypy>=0.0.10
scanorama>=1.7.0
bbknn>=1.6.0
# NOTE: scvi-tools releases frequently introduce breaking changes.
# Lock to <1.3.0 for stability; test with newer versions before upgrading.
scvi-tools>=1.1.0,<1.3.0

# Additional dependencies
scipy>=1.11.0
scikit-learn>=1.4.0
leidenalg>=0.10.0

# R dependencies (install via R console):
# install.packages("Seurat")          # V5 by default
# remotes::install_version("Seurat", version = "4.4.0")  # For V4
# install.packages("harmony")
# BiocManager::install("batchelor")
# install.packages("dplyr")
# BiocManager::install("lisi")
